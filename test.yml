tables:
  - name: sentry_organization
    columns:
      - name: id
        type: int
      - name: name
        type: string
      - name: slug
        type: string
      - name: status
        type: int
      - name: date_added
        type: string
      - name: default_role
        type: string
      - name: is_test
        type: boolean
      - name: flags
        type: int
      - name: customer_id
        type: string